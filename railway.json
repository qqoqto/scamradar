{
  "deploy": {
    "startCommand": "python -c \"import os; port=os.environ.get('PORT','8000'); os.system(f'uvicorn app.main:app --host 0.0.0.0 --port {port} --log-level info')\"",
    "healthcheckPath": "/health"
  }
}